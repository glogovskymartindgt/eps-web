export enum TableFilterType {
    DATE = 'date',
    DATERANGE = 'dateRange',
    DATETIME_AS_DATERANGE = 'dateTimeAsDateRange',
    SELECT = 'select',
    SELECT_STRING = 'selectString',
    SELECT_NUMBER = 'selectNumber',
    SELECT_ANY_DATE_TIME = 'selectAnyDateTime',
    TYPE = 'type',
    NUMBER = 'number',
    STRING = 'string',
    CUSTOM = 'custom',
}
